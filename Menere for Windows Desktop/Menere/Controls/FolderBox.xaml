<UserControl x:Class="Menere.Controls.FolderBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <VirtualizingStackPanel Orientation="Horizontal" Margin="4,2,2,0">
            <Image Name="image_folder" Source="{DynamicResource image_folder}" Margin="0,0,5,0" Height="12"  />
            <TextBlock Name="textblock_name" Background="Transparent" Text="{Binding Path=name}" ToolTip="{Binding Path=name}" />
        </VirtualizingStackPanel>
    </Grid>
</UserControl>
